# Podman: resolve unqualified image names (e.g. `redis`, `adminer`) against
# Docker Hub — matching Docker's behavior, which docker-compose files assume.
# Without this, Podman errors: "short-name ... did not resolve to an alias, and
# no unqualified search registries are defined".
unqualified-search-registries = ["docker.io"]
